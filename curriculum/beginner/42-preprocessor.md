# 42 — Preprocessor

دستورهایی مثل `#include` پیش از مرحله اصلی compilation پردازش می‌شوند.

Header guard یا `#pragma once` از include تکراری جلوگیری می‌کند.

Macroها ابزار قدرتمند ولی بدون type safety هستند؛ در Modern C++ اغلب constexpr/template جایگزین بهتری‌اند.
