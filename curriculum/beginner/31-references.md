# 31 — Reference

Reference نام دیگری برای یک object موجود است.

```cpp
int value{10};
int& ref = value;
ref = 20;
```

پس از تغییر `ref` مقدار `value` نیز 20 است.

## تمرین
تابعی بنویسید که دو integer را با reference جابه‌جا کند.
