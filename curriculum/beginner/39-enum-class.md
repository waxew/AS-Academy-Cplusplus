# 39 — enum class

```cpp
enum class Status { Pending, Active, Done };
```

`enum class` scoped است و از بسیاری از تبدیل‌های ناخواسته enum سنتی جلوگیری می‌کند.

## تمرین
وضعیت سفارش را با enum class مدل کنید.
