# 26 — Function Overloading

چند تابع می‌توانند نام یکسان ولی signature متفاوت داشته باشند.

```cpp
int add(int, int);
double add(double, double);
```

## تمرین
تابع `maxValue` را برای `int` و `double` overload کنید.
