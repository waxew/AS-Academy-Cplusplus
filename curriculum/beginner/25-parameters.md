# 25 — پارامترها

Pass by value یک copy منطقی می‌دهد. Pass by reference به object اصلی متصل است. `const T&` برای خواندن objectهای بزرگ مناسب است.

## تمرین
تابعی برای چاپ اطلاعات `std::string` با `const reference` بنویسید.
