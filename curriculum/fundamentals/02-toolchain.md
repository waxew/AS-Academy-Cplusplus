# 02 — Toolchain

Toolchain معمولاً شامل Compiler، Linker، Debugger و Build System است. GCC، Clang و MSVC سه خانواده مهم Compiler هستند.

```bash
g++ --version
```

در این دوره CMake برای Build قابل تکرار استفاده می‌شود.

## تمرین
نسخه Compiler سیستم خود را پیدا و یادداشت کنید.
