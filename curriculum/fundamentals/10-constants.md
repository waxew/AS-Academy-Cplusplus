# 10 — const و constexpr

```cpp
const int maxUsers{100};
constexpr double pi{3.141592653589793};
```

`const` مانع تغییر از طریق آن نام می‌شود. `constexpr` برای مقادیری است که می‌توانند در compile time ارزیابی شوند.

## تمرین
مساحت دایره را با یک ثابت `pi` محاسبه کنید.
