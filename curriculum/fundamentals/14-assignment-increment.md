# 14 — Assignment و Increment

`=`, `+=`, `-=`, `*=`, `/=` برای به‌روزرسانی مقدار استفاده می‌شوند. `++` و `--` مقدار را یک واحد تغییر می‌دهند.

## تمرین
یک counter بسازید و چند بار مقدار آن را تغییر دهید.
