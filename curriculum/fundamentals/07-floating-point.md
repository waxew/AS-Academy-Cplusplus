# 07 — اعداد اعشاری

`float`, `double`, `long double` برای مقادیر اعشاری‌اند. بسیاری از اعداد دهدهی در binary دقیق نمایش داده نمی‌شوند.

```cpp
double value{0.1};
```

برای مقایسه floating-point معمولاً نباید انتظار برابری دقیق داشت.

## تمرین
نتیجه `0.1 + 0.2` را با دقت زیاد چاپ کنید.
