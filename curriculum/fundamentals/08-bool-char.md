# 08 — bool و char

`bool` دو حالت منطقی دارد. `char` یک code unit کوچک برای متن/داده است.

```cpp
bool loggedIn{false};
char grade{'A'};
```

## تمرین
برنامه‌ای بنویسید که وضعیت قبولی را با bool نگه دارد.
