# 06 — انواع صحیح

انواع رایج: `short`, `int`, `long`, `long long` و نسخه‌های unsigned.

اندازه دقیق همه این typeها روی همه پلتفرم‌ها یکسان تضمین نشده است. برای اندازه ثابت می‌توان از `<cstdint>` مثل `std::int32_t` استفاده کرد.

## تمرین
با `sizeof` اندازه چند نوع را روی سیستم خود چاپ کنید.
