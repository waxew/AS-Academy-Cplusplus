# 03 — اولین برنامه

```cpp
#include <iostream>

int main() {
    std::cout << "Hello C++\n";
    return 0;
}
```

`main` نقطه شروع برنامه مستقل است. `std::cout` خروجی استاندارد را می‌نویسد.

## تمرین
متن خروجی را به نام خودتان تغییر دهید.
