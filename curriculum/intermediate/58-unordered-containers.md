# 58 — unordered_map / unordered_set

این containerها hash-based هستند و lookup میانگین O(1) دارند، اما ترتیب مرتب تضمین نمی‌کنند.

انتخاب map یا unordered_map باید بر اساس نیاز ordering، hash و performance واقعی باشد.
