# 50 — Composition

Composition یعنی یک object، objectهای دیگری را به‌عنوان جزء نگه دارد.

مثال: `Car` یک `Engine` دارد، نه اینکه `Car` نوعی `Engine` باشد.

Composition coupling را اغلب کمتر از inheritance می‌کند.
