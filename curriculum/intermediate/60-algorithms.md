# 60 — Algorithms

کتابخانه `<algorithm>` شامل `sort`, `find`, `find_if`, `count_if`, `transform` و بسیاری ابزارهای دیگر است.

قاعده خوب: قبل از نوشتن loop دستی بررسی کنید آیا algorithm استاندارد مسئله را بیان می‌کند.
