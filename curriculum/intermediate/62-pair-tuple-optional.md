# 62 — pair، tuple و optional

`std::pair` دو مقدار، `std::tuple` چند مقدار heterogeneous و `std::optional<T>` وجود یا عدم وجود یک مقدار را مدل می‌کند.

`optional` اغلب بهتر از sentinel value مبهم است.
