# 95 — HTTP و REST Client

Client حرفه‌ای باید status code، timeout، retry policy، TLS validation و malformed response را مدیریت کند.

Retry کورکورانه برای عملیات non-idempotent می‌تواند خطرناک باشد.
