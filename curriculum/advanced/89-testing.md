# 89 — Testing

Unit test یک واحد کوچک رفتار را بررسی می‌کند. Integration test تعامل چند جزء را می‌سنجد.

Test خوب deterministic، مستقل و دارای failure message قابل فهم است.

در این repo CTest یک smoke test پایه اجرا می‌کند.
