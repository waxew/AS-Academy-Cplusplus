# 82 — filesystem و chrono

`std::filesystem` برای path/file operations و `std::chrono` برای زمان و duration استفاده می‌شود.

از integer خام برای واحد زمان اجتناب کنید؛ typeهای duration intent را روشن می‌کنند.
