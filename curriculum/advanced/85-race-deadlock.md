# 85 — Race Condition و Deadlock

Data race روی داده غیراتمیک undefined behavior است. Deadlock زمانی رخ می‌دهد که threadها برای resourceهای یکدیگر منتظر بمانند.

ترتیب ثابت lock و کاهش shared mutable state از راهکارهای مهم‌اند.
