# 88 — future و async

`std::future` نتیجه‌ای را مدل می‌کند که بعداً آماده می‌شود. `std::async` می‌تواند اجرای callable را مدیریت کند.

برای API concurrent، lifetime و cancellation strategy را نیز طراحی کنید.
