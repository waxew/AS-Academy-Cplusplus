# 74 — RAII

Resource Acquisition Is Initialization مالکیت resource را به lifetime object متصل می‌کند.

File، mutex lock، memory و socket می‌توانند با این الگو مدیریت شوند.

RAII یکی از بنیادی‌ترین idiomهای C++ است.
