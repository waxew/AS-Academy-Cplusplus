# 107 — پروژه نهایی Store Manager

C++20 + Qt + SQLite + CMake.

ماژول‌ها: Customer، Product، Inventory، Invoice، Payment، Reports، Backup/Restore.

معماری: UI -> Application -> Domain -> Repository -> SQLite.

پروژه با unit/integration tests، Release build و مستندات تحویل کامل می‌شود.
