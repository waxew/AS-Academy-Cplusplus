# Secure C++

Buffer overflow، integer overflow، dangling pointer، use-after-free، double free، UB، validation، safe APIs، sanitizers، fuzzing fundamentals و threat-aware error handling.

ابزارها: AddressSanitizer، UndefinedBehaviorSanitizer، ThreadSanitizer و static analysis.
